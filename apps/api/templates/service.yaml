apiVersion: v1
kind: Service
metadata:
  name: api
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - port: { { .Values.service.port } }
      targetPort: { { .Values.service.targetPort } }
